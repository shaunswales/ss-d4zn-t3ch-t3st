Cypress.Commands.add('checkTheWeather', (postcode) => {
  // Search for Postcode
  // Switch
  // Weather Details
  // Get JSON Response  
  // Assert Results
});

Cypress.Commands.add(
  'checkTheWeatherInvalid',
  (postcode, expectedOutcome) => {},
);
